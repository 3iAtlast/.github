txt: 
cname: 
google-site:verifacation=iox
8qqkaoivOCKCaYdvftoEpxaZFGyYi403Xejc8K94,

        ranges::initializer_list,(c++11),
        ranges::in_fun_results(c++20)
        ranges::in_in_results(c++20)              ranges::in_out_results(c++20)             ranges::in_out_out_results(c++20)
        ranges::min_max_results(c++20)            ranges::in_found_results(c++20)           ranges::in_value_results(c++23)
        ranges::out_value_results(c++23)
search,

        ranges::search_n(c++20)        
        
        
Modifying Sequence Operation,
copy,
copy_if(c++11),

        ranges:©️
        
        ranges::copy_if(c++20),
        
copy_n(c++11),

        ranges_copy_n(c++20),
        
copy_backward,

        ranges::copy_backward(c++20),
        
move(c++11),

        ranges::move(c++20),
        
move_backward,

        ranges::move_backward(c++20),
        
for_each_n(c++20),

        ranges::for_each_n(c++20),
        
count,
count_if,

        ranges::count(c++20)
        ranges::count_if(c++20)
        
       
